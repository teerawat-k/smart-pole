# T01 · Influx plugin — connection + write/query API wrapper

Epic: E08

## Expected
- `src/plugins/influx.ts` — singleton `InfluxDB` client
- export `getWriteApi()`, `getQueryApi()`, `Point`
- env: `INFLUXDB_URL`, `INFLUXDB_TOKEN`, `INFLUXDB_ORG`, `INFLUXDB_BUCKET`
- writeApi: `flushInterval: 5000, batchSize: 100` (ไม่ flush ทุก write)
- error handler — log + don't throw (best effort)

## Test Guide
- [ ] write point → query กลับมาเจอ
- [ ] batch flush ทำงาน
- [ ] connection retry
