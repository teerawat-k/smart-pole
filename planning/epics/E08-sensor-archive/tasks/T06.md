# T06 · Hypertable `sensor_heartbeat_signal` (signal history)

Epic: E08

## Expected
- model `SensorHeartbeatSignal`:
  ```prisma
  time         DateTime
  poleId       Int
  signalDbm    Int      // -120..0
  uptimeSec    BigInt
  firmwareVer  String?
  ingestedAt   DateTime @default(now())
  @@id([time, poleId])
  @@index([poleId, time(sort: Desc)])
  ```
- ingest จาก `smartpole/+/heartbeat` (E06.T04) ผ่าน registry handler key `heartbeat_signal`
- ใช้สำหรับ chart signal strength + uptime ของ pole

## Test Guide
- [ ] heartbeat → 1 row
- [ ] history chart (E15.T06) ใช้ data
