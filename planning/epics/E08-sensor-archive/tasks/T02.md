# T02 · Sensor archive module — write + getLatest + getHistory

Epic: E08

## Expected
- `src/modules/sensor-archive/`
  - `sensor-archive.service.ts` — orchestrator
  - `flow/write-sensor.ts` — atom: build Point → writeApi
  - `flow/get-latest.ts` — atom: Flux query `last()`
  - `flow/get-history.ts` — atom: Flux query range + pivot
  - `sensor-archive.controller.ts`:
    - `GET /api/poles/:poleName/sensor/latest`
    - `GET /api/poles/:poleName/sensor/history?from&to&interval=5m` (interval optional, ถ้าใส่ → window aggregate)
- ใช้ poleName (ไม่ใช่ id) เพราะ Influx tag

## Test Guide
- [ ] write → getLatest return ที่ write
- [ ] history range filter ถูก
- [ ] interval=5m → return windowed data
- [ ] poleName ที่ไม่มี data → empty array
- [ ] integration test กับ Influx จริง
