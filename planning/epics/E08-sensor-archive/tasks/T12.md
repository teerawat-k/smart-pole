# T12 · CSV export (frontend gen)

Epic: E08

## Expected
- backend ส่ง raw rows — frontend gen CSV
- check permission `sensor_archive:canExport`
- backward-compatible: history endpoint ส่ง field ทั้งหมดของ sensor (พร้อม flag canExport)

## Test Guide
- [ ] frontend export ทำงาน
- [ ] no permission → ปุ่มซ่อน
