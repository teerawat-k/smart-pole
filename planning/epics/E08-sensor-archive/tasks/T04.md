# T04 · CSV export endpoint (ส่ง raw — frontend gen file)

Epic: E08

## Expected
- ไม่มี backend endpoint export — frontend ดึง history → gen CSV เอง (ตามมาตรฐาน)
- แต่ต้องตรวจ permission `sensor_archive:canExport` ตอน frontend แสดงปุ่ม
- backend list endpoint ส่ง flag `permissions.canExport` มาด้วย

## Test Guide
- [ ] role ที่ไม่มี canExport → ไม่เห็นปุ่ม
- [ ] role มี canExport → ปุ่มทำงาน + frontend gen file
