# T03 · Aggregation queries — daily avg / max / min

Epic: E08

## Expected
- `flow/get-daily-summary.ts` — Flux: aggregateWindow(every: 1d, fn: mean) + max + min
- endpoint `GET /api/poles/:poleName/sensor/summary?from&to&granularity=daily|hourly`

## Test Guide
- [ ] 7 วัน data → return 7 rows daily
- [ ] hourly → 168 rows
