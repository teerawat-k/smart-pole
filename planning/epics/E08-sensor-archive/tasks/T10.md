# T10 · CLI tool — `bun scripts/new-sensor.ts <key>`

Epic: E08

## Expected
- generate ครบ:
  - Prisma model `Sensor<Pascal>` ใน schema
  - migration custom SQL (hypertable + index + 3 continuous aggregates + retention policy)
  - module folder `src/modules/sensor-<key>/` ตาม pattern (controller/service/repository/schema/constants/index/test)
  - handler file `src/modules/sensor-<key>/handler.ts` + register
  - import + register ใน `src/plugins/mqtt/index.ts`
  - seed entry ใน `prisma/seeds/sensor-types.ts`
- prompt ถาม fields ที่ต้องการ: `(name, type, unit, range)` — gen Zod + Prisma column
- example: `bun scripts/new-sensor.ts noise --field=db:Float:"30..130" --field=peak:Float?`

## Test Guide
- [ ] new-sensor noise → 1 migration + 1 module + 1 handler + 1 seed entry + ลงทะเบียนใน mqtt
- [ ] `bunx tsc --noEmit` ผ่านทันที
- [ ] migration apply + table ใหม่เป็น hypertable
