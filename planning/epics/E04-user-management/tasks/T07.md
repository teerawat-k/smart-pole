# T07 · Block delete user ตัวเอง + last admin

Epic: E04

## Expected
- `flow/check-user-deletable.ts` atom
  - block ถ้า id === request.userId → 403 "ห้ามลบตัวเอง"
  - block ถ้า role=admin + เป็น admin ที่ active คนสุดท้าย → 403 "ต้องมี admin อย่างน้อย 1 คน"
- list flag `canDelete: false` ในกรณีนี้

## Test Guide
- [ ] ลบตัวเอง → 403
- [ ] ลบ admin คนเดียว → 403
- [ ] ลบ admin คนหนึ่งใน 2 คน → ผ่าน
