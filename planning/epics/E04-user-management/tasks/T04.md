# T04 · `/users/:id/reset-password` — admin reset

Epic: E04

## Expected
- `POST /api/users/:id/reset-password` body `{ newPassword }` (admin เท่านั้น)
- Hash + save + invalidate refresh tokens (logout-all)
- audit log `USER_RESET_PASSWORD`

## Test Guide
- [ ] reset → user คนนั้น login ด้วย password ใหม่ได้
- [ ] refresh token เก่าใช้ไม่ได้
