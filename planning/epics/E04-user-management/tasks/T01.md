# T01 · Prisma schema User — soft delete + audit + lockout

Epic: E04

## Expected
- เพิ่ม model `User` ครบ field
  - identity: `username @unique`, `email @unique`, `firstName`, `lastName`, `mobileNo`
  - auth: `passwordHash`, `passwordChangedAt`, `loginFailCount`, `lockedUntil`, `lockedReason`, `lastLoginAt`
  - relation: `roleId`
  - standard: `isActive`, `deletedAt`, `createdBy`, `updatedBy`, `deletedBy`, `createdAt`, `updatedAt`
- Indexes: `username`, `email`, `(deletedAt)`, `(roleId, deletedAt)`

## Test Guide
- [ ] migration apply
- [ ] soft delete: `deletedAt` set แต่ row อยู่
- [ ] cannot create username duplicate
