# T05 · `/users/:id/unlock` — clear locked state

Epic: E04

## Expected
- `POST /api/users/:id/unlock` (admin)
- clear `loginFailCount=0, lockedUntil=null, lockedReason=null`
- audit `USER_UNLOCK`

## Test Guide
- [ ] user lock → admin unlock → login ได้
