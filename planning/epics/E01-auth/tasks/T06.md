# T06 · Lockout policy + scheduled unlock job

Epic: E01
Status: todo
Blocked by: E01.T02

## Expected
- Cron job (`*/5 * * * *`) — clear expired lockouts (`lockedUntil < now()` → set null + reset `loginFailCount = 0` ถ้า > 0)
- ใช้ `node-cron` หรือ `setInterval` (ภายใน Elysia plugin `src/plugins/scheduler.ts`)
- Postgres advisory lock กัน job ซ้อน (รองรับ multi-instance)

## Test Guide
- [ ] user lockedUntil < now → clear หลัง job รัน
- [ ] user ที่ lockedReason = `too_many_attempts` + isActive=false → ไม่ unlock อัตโนมัติ (admin enable เอง)
- [ ] 2 instance run พร้อมกัน → job รันแค่ตัวเดียว (advisory lock)

## Notes
- Job log: `auditService.log({ action: 'AUTO_UNLOCK', detail: { unlockedCount } })`
