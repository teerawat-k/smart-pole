# T01 · System log page
Epic: E22

## Expected
- columns: createdAt, logType, username, ipAddress, action/reason
- filter: logType (combobox), userId (lookup), date range
- sort by createdAt desc
- detail dialog → JSON viewer
