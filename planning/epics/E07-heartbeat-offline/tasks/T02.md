# T02 · Offline detection job

Epic: E07

## Expected
- cron `*/1 * * * *` — query `Pole WHERE lastSeenAt < now() - threshold AND poleStatus = 'online' AND deletedAt IS NULL`
- batch update → status='offline' + emit alert + WS broadcast
- audit log + metric `pole_offline_detected_total`

## Test Guide
- [ ] pole ไม่ส่ง heartbeat 4 นาที → mark offline
- [ ] pole ที่ status=offline แล้ว ไม่ trigger ซ้ำ
- [ ] pole ที่ maintenance → skip
- [ ] integration test ใช้ time travel (mock now)
