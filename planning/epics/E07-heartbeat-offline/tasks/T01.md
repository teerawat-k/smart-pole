# T01 · Scheduler plugin — cron + advisory lock

Epic: E07

## Expected
- `src/plugins/scheduler.ts` — wrap `node-cron`
- helper `runWithLock(lockKey: number, fn)` ใช้ Postgres `pg_try_advisory_lock`
- registry ของ jobs ที่ run อยู่ — `GET /admin/jobs` list

## Test Guide
- [ ] 2 instance → job ตัวเดียว run
- [ ] lock release หลัง fn จบ (success/error)
- [ ] manual trigger ผ่าน endpoint admin
