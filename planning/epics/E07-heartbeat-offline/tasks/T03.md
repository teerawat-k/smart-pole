# T03 · Pole heartbeat history (Influx)

Epic: E07

## Expected
- เพิ่ม atom `flow/write-heartbeat-point.ts` — write Influx measurement `pole_heartbeat` tag `pole_id` field `signal_dbm, uptime_sec, is_online`
- query API: `GET /api/poles/:id/heartbeat?from&to` → array of points (สำหรับ graph)

## Test Guide
- [ ] heartbeat → 1 point in Influx
- [ ] history query return correct range
