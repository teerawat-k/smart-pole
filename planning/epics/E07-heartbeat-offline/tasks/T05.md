# T05 · Threshold config — DB-backed (ไม่ hardcode)

Epic: E07

## Expected
- model `SystemConfig`: `key @unique`, `value Json`, `updatedBy`, `updatedAt`
- seed key:
  - `pole.offline_threshold_minutes`: 3
  - `alert.deduplicate_window_seconds`: 60
  - `mqtt.timestamp_drift_max_seconds`: 300
- `GET/PATCH /api/admin/configs` — admin only
- service ต่าง ๆ อ่านผ่าน `configService.get(key)` (cached 30s)

## Test Guide
- [ ] update threshold → reflect ใน scan job ครั้งถัดไป
- [ ] cache TTL ทำงาน
