# T02 · Auth guard layout

Epic: E14

## Expected
- `app/(dashboard)/layout.tsx` — client component
- รัน `useRouteGuard()` — ถ้า `!user` → redirect `/login`
- ถ้า user มี + เข้าหน้าที่ไม่มี permission → redirect `/forbidden`
- show loading skeleton ระหว่าง rehydrate auth store

## Test Guide
- [ ] ไม่ login → redirect login
- [ ] login + ไม่มี permission → forbidden page
- [ ] login + ปกติ → render
