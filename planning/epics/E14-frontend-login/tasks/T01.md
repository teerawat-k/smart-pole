# T01 · Login page

Epic: E14

## Expected
- `app/(auth)/login/page.tsx`
- Form: username, password, captcha image + input
- บน mount: call `GET /api/captcha/new` → render image
- submit → `POST /api/auth/login` → save tokens + user + permissions ใน authStore → redirect `/dashboard`
- error handling:
  - invalid → toast + refresh captcha
  - locked → show countdown timer + thai message
  - disabled → "บัญชีถูกปิดใช้งาน กรุณาติดต่อผู้ดูแล"

## Test Guide
- [ ] login flow ครบ
- [ ] captcha refresh หลัง fail
- [ ] countdown ทำงาน + auto enable submit เมื่อหมด
