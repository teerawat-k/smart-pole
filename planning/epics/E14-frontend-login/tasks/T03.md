# T03 · Sidebar dynamic menu

Epic: E14

## Expected
- `components/layout/app-sider/`
- menu = static config + filter ด้วย `hasPermission(moduleKey, 'canView')`
- highlight active route
- mobile drawer + desktop sidebar
- logout button + user info

## Menu Map
- `/dashboard` ← `dashboard:canView`
- `/camera` ← `camera_archive:canView`
- `/sensor` ← `sensor_archive:canView`
- `/poles` ← `pole:canView`
- `/users` ← `user:canView`
- `/roles` ← `role:canView`
- `/alerts` ← `alert:canView`
- `/logs/system` ← `system_log:canView`
- `/logs/audit` ← `audit_log:canView`
- `/profile` ← public (any logged-in)

## Test Guide
- [ ] user role → เห็นแค่ dashboard/camera/sensor/profile
- [ ] admin → เห็นทุก menu
- [ ] active highlight
