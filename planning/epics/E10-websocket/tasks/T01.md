# T01 · WS plugin — JWT auth + connection registry

Epic: E10

## Expected
- `src/plugins/websocket.ts`:
  - `addConnection(userId, ws)`, `removeConnection(userId, ws)`
  - JWT verify ตอน open (token ใน query string)
  - close 4001 ถ้า invalid token
- mount `/ws?token=...` ใน `index.ts`
- log connection counts

## Test Guide
- [ ] connect ด้วย token valid → success
- [ ] invalid token → close 4001
- [ ] map clean เมื่อ disconnect
