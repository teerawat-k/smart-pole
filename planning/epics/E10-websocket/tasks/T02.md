# T02 · Broadcaster atoms

Epic: E10

## Expected
- `src/plugins/websocket.ts` export:
  - `sendInvalidate(userIds: number[], entity: string)` → frontend invalidate query key `[entity]`
  - `sendNotification(userIds, payload)` → toast + bell
  - `broadcastToAll(message)` (sparingly, e.g. system maintenance)
  - `sendToRole(roleName, message)` (resolved through cache)

## Test Guide
- [ ] invalidate → frontend get message
- [ ] sendToRole — ส่งให้ทุก user ที่ role นั้น online
