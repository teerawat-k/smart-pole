# T04 · Reconnect-friendly protocol + sequence id

Epic: E10

## Expected
- ทุก message มี `seq` (incremental per user)
- frontend resync: ส่ง `{ type: 'resync', lastSeq }` ตอน reconnect → server ส่ง message ที่ `seq > lastSeq` (buffer 100 last messages per user)

## Test Guide
- [ ] disconnect 5s + reconnect → ได้ message ที่ขาด
- [ ] disconnect 5 min → buffer overflow → ส่ง force-refetch
