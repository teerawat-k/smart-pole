# T01 · Prisma schema Pole

Epic: E05

## Expected
- model `Pole`:
  - identity: `poleName @unique`, `installPlace`, `ddnsHostname @unique`, `ipCamera?`
  - geo: `latitude Decimal(10,7)?`, `longitude Decimal(10,7)?`
  - capability: `hasCamera`, `hasPm25Sensor`, `hasTempHumidity`, `hasLed`
  - status: `poleStatus` (enum), `lastSeenAt?`
  - mqtt: `mqttUsername @unique`, `mqttPasswordHash`
  - standard: isActive, deletedAt, createdBy/updatedBy/deletedBy, timestamps
- Postgres enum `PoleStatus { online offline unknown maintenance }`
- Indexes: `poleName`, `ddnsHostname`, `mqttUsername`, `(poleStatus, lastSeenAt)`, `(deletedAt)`

## Test Guide
- [ ] migration apply
- [ ] enum value validate
- [ ] lat range -90..90, lng -180..180 (validate ที่ schema layer ใน T02)
