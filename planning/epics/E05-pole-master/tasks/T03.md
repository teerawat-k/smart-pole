# T03 · Status enum + status update flow

Epic: E05

## Expected
- `flow/update-status.ts` atom — `setStatus(poleName, status, tx?)` + update lastSeenAt
- ใช้จาก MQTT handler (E06) + heartbeat job (E07)
- ห้าม expose endpoint แก้ status ตรง — admin ใช้ `/maintenance` แทน
- `POST /api/poles/:id/maintenance` body `{ enabled: boolean, reason }` → status='maintenance' / กลับ status เดิม

## Test Guide
- [ ] update status → record updated
- [ ] update lastSeenAt
- [ ] maintenance toggle ทำงาน + audit log
- [ ] WebSocket invalidate `pole-status`
