# T02 · Pole module — CRUD + lookup + pagination

Epic: E05

## Expected
- `src/modules/pole/` ตาม pattern
- Endpoints CRUD + `/lookup` + `/toggle-active`
- list filter: `poleStatus`, `hasCamera`, `isActive`, `search` (poleName/installPlace)
- list response: include `lastSeenAt`, `poleStatus`, capability flags + permission flags (canEdit/canDelete)
- lookup: `id, poleName, installPlace, capability flags` (combobox dashboard ต้องรู้ว่ามี sensor อะไรเปิดทำงาน)

## Test Guide
- [ ] CRUD ครบ
- [ ] poleName + ddnsHostname + mqttUsername unique check
- [ ] filter by status
- [ ] integration test
