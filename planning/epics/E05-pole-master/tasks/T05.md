# T05 · Block delete pole ที่มี recording/alert ค้าง

Epic: E05

## Expected
- `flow/check-pole-deletable.ts` atom — block ถ้ามี:
  - `videoRecording` ที่ `isAvailable: true` (อย่างน้อย 1)
  - `alert` ที่ `isResolved: false`
- service throw `ConflictError("POLE-002", "ลบเสาไม่ได้เพราะมีข้อมูลที่ยังไม่ได้ลบหรือ alert ค้างอยู่")`
- list flag `canDelete: false`

## Test Guide
- [ ] pole ไม่มี recording → ลบได้
- [ ] pole มี recording active → 409
- [ ] pole มี alert ค้าง → 409
