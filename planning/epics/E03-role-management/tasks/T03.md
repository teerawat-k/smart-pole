# T03 · Block delete role ที่มี user อ้างถึง

Epic: E03

## Expected
- `flow/check-role-deletable.ts` atom — return false ถ้ามี user.roleId อ้างถึง
- service `delete()` throw `ConflictError("ROLE-002", "ลบ Role นี้ไม่ได้เพราะมีผู้ใช้งานอยู่")`

## Test Guide
- [ ] role ไม่มี user → ลบได้
- [ ] role มี user → 409 + thai message
- [ ] frontend list มี flag `canDelete: false` ถ้ามี user
