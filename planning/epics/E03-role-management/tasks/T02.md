# T02 · Permission flow — replacePermissions (transaction)

Epic: E03

## Expected
- `flow/replace-permissions.ts` atom — รับ `(roleId, permissions[], tx?)`
- ใน `$transaction`: deleteMany permissions ของ roleId → createMany ใหม่
- emit `permissionChanged(roleId)` หลัง commit

## Endpoint
- `PUT /api/roles/:id/permissions` body `{ permissions: [{ moduleId, canView, canCreate, ... }] }`

## Test Guide
- [ ] ส่ง 5 permissions → DB มี 5 rows
- [ ] ส่ง 0 → DB clear permissions ทั้งหมดของ role
- [ ] tx fail → rollback
- [ ] cache invalidate
