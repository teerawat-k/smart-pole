# T01 · Role module — CRUD + lookup + toggle-active

Epic: E03

## Expected
- `src/modules/role/` ครบตาม pattern
- Endpoints CRUD + `/lookup` + `/toggle-active` + `/reorder` (ถ้าต้อง)
- SELECTs `ROLE_LIST_SELECT` (with `_count.users`), `ROLE_DETAIL_SELECT` (with permissions), `ROLE_LOOKUP_SELECT`
- List include flag `canDelete` (false ถ้า isSystem หรือมี user อ้างถึง)

## Test Guide
- [ ] CRUD ครบ + audit
- [ ] list มี user count
- [ ] lookup id+displayName
- [ ] toggle active → user ที่ใช้ role นี้ login ไม่ได้
