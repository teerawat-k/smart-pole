# T04 · Error system — AppError + subclasses + ErrorCode catalog

Epic: E00
Status: done
Blocked by: T01

## Expected
- `src/common/errors/app-error.ts` — `AppError(statusCode, code, message)` + subclasses
- `src/common/errors/codes.ts` — `ErrorCode` รูปแบบ `MODULE-NNN`
- onError ใน `index.ts` map AppError → response มาตรฐาน + thai message + requestId

## Summary
- Error subclasses ครบ 7 ตัว: NotFound (404), Duplicate (409), Validation (400), **Unauthorized (401 — เพิ่มใหม่)**, Forbidden (403), Conflict (409), RateLimit (429)
- ErrorCode catalog แยกตาม module: COMMON, AUTH, USER, ROLE, MODULE, POLE, SENSOR, RECORDING, ALERT (ทั้งหมด 38 codes)
- onError (index.ts ทำใน T01) map ครบ — `requestId` ใส่ใน response (ทำใน T03)

## Test Result
- `bun test src/common/errors/app-error.test.ts` → **11 pass / 0 fail** (44ms, 63 expect calls)
  - AppError: statusCode/code/message + instanceof Error
  - 7 subclasses: statusCode + name correct
  - ErrorCode: pattern `MODULE-NNN` + ไม่มี code ซ้ำ
- `bunx tsc --noEmit` 0 errors

## Notes
- Service ใช้ `throw new NotFoundError(ErrorCode.POLE_NOT_FOUND, "ไม่พบเสาที่ระบุ")`
- Controller ห้าม throw — ปล่อย service throw + Elysia onError handle
- Error message ภาษาไทยทั้งหมด
