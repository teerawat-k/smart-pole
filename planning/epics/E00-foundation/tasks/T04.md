# T04 · Error system — AppError + subclasses + ErrorCode catalog

Epic: E00
Status: todo
Blocked by: T01

## Expected
- `src/common/errors/app-error.ts` — `AppError(statusCode, code, message)` + 6 subclasses (NotFound, Duplicate, Validation, Forbidden, Conflict, RateLimit)
- `src/common/errors/codes.ts` — `ErrorCode` constant object รูปแบบ `MODULE-NNN`
- `src/common/errors/index.ts` — re-export
- onError ใน `index.ts` map AppError → response มาตรฐาน

## API Response (error format)
```json
{ "success": false, "error": { "code": "POLE-001", "message": "ไม่พบเสาที่ระบุ", "requestId": "uuid" } }
```

## Test Guide
- [ ] throw `NotFoundError("POLE-001", "msg")` → response 404 + code + message
- [ ] throw error ทั่วไป → response 500 + code `INTERNAL_ERROR` + log error stack
- [ ] message ภาษาไทย (ห้าม English)
- [ ] unit test ทุก subclass (statusCode + name)

## Notes
- ทุก service throw subclass ที่เหมาะสม — ห้าม throw `AppError` ดิบ
- ห้าม throw ใน controller — ปล่อยให้ service throw แล้ว Elysia onError จัดการ
