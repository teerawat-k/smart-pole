# T06 · Common utils — prisma-tx, date, password (argon2id), reorder

Epic: E00
Status: done
Blocked by: T02

## Expected
- `prisma-tx.ts` — `PrismaTx` type (`Prisma.TransactionClient | typeof prisma`)
- `pagination.ts` — `toSkipTake()` helper (ทำใน T05)
- `date.ts` — `parseOptionalDate`, `calcDaysDiff`, `dayRange` (Asia/Bangkok)
- `password.ts` — `hashPassword`, `verifyPassword` (argon2id)
- `reorder.ts` — `reorderRecord()` ใช้ใน DnD
- `index.ts` — re-export ทุก util

## Summary
- ใช้ argon2 npm package (native module) — `hash()` + `verify()` พร้อม catch ใน verify เพื่อ return false (ไม่ throw)
- date helpers: parseOptionalDate / calcDaysDiff / dayRange (Asia/Bangkok UTC+7)
- reorder helper: ตรงกับของ pmk-psom-v2-remark — orderField default `"order"` (ไม่ใช่ `"sortOrder"`)
- ยังไม่ทำ scope-check (โปรเจคนี้ permission scope ง่ายกว่า — ใช้ ระดับ module:action เพียงระดับเดียว ไม่มี owned/in-branch/in-area)

## Test Result
- `bun test src/common/utils/` → **14 pass / 0 fail** (746ms)
  - password.test.ts: 5 cases (hash format / verify correct/wrong / invalid hash / re-hash same plain different output)
  - date.test.ts: 9 cases (parseOptionalDate × 3 / calcDaysDiff × 3 / dayRange × 3)
- `bun test` (all backend) → **50 pass / 0 fail** (115 expect calls)
- `bunx tsc --noEmit` 0 errors

## Notes
- argon2 cost: ใช้ default ของ argon2 npm — argon2id (memoryCost 65536KB, timeCost 3, parallelism 4)
- เผื่อ legacy bcrypt: auth flow `verify-password.ts` (E01.T02) จะ try argon2 → fallback bcrypt → re-hash เป็น argon2
- scope-check: skip — เพิ่มทีหลังถ้า business ต้องการ multi-level scope
