# T06 · Common utils — prisma-tx, date helpers, password (argon2id)

Epic: E00
Status: todo
Blocked by: T02

## Expected
- `src/common/utils/prisma-tx.ts` — `PrismaTx` type
- `src/common/utils/pagination.ts` — `toSkipTake(query)` helper
- `src/common/utils/date.ts` — `parseOptionalDate`, `dayRange(date)` (start-of-day, end-of-day Asia/Bangkok)
- `src/common/utils/password.ts` — `hashPassword(plain)` + `verifyPassword(plain, hash)` ใช้ argon2id
- `src/common/utils/scope-check.ts` — generic guard `assertOwnership` (ใช้ใน service)

## Test Guide
- [ ] hash → length > 60, ขึ้นต้นด้วย `$argon2id$`
- [ ] verify with wrong password → false
- [ ] verify with right password → true
- [ ] dayRange("2026-04-27") return `{ start: 2026-04-26T17:00Z, end: 2026-04-27T16:59:59Z }` (UTC for Asia/Bangkok)
- [ ] unit test ครบทุก function

## Notes
- argon2 native module ต้อง compile ตอน install — ตรวจ Docker base image
- เผื่ออนาคต legacy login: รับ bcrypt verify ได้ + re-hash → argon2 ตอน login สำเร็จ (ทำใน E01)
