# T01 · Backend bootstrap — Elysia + env + onError + health

Epic: E00
Status: done
Blocked by: —

## Expected
- ติดตั้ง Bun + Elysia + dependencies (`@elysiajs/cors`, `swagger`, `static`, `jwt`, `bearer`)
- `src/config/env.ts` — Zod validate ทุก env
- `src/index.ts` — Elysia app + cors + swagger + static + onError handler
- `GET /health` (liveness) + `GET /health/ready` (readiness — ตรวจ DB)
- Graceful shutdown: SIGTERM/SIGINT
- `package.json` scripts: `dev`, `start`, `kill-port`, `setup`

## API
- `GET /health` → `{ success: true, data: { status: "ok" } }`
- `GET /health/ready` → `{ success: true, data: { db: "ok" } }` / 503 ถ้า DB down

## Summary
- ติดตั้ง dependencies ครบ (`bun install` 187 packages)
- ขยาย `config/env.ts` รวม MQTT/SRS/storage env + helper `isDev/isProd/isTest`
- เขียน `index.ts` พร้อม `/health`, `/health/ready` (ใช้ `pingDb()` query `SELECT 1`)
- onError จัดการ NOT_FOUND / VALIDATION / AppError / 5xx + response มาตรฐาน + ภาษาไทย
- graceful shutdown: SIGTERM/SIGINT → `app.stop()` → `prisma.$disconnect()` → exit
- production: `uncaughtException`, `unhandledRejection` handler
- prisma plugin: PrismaPg adapter, singleton, Decimal→number
- Prisma 7 config (`prisma.config.ts`) ตาม pmk-psom-v2-remark style — โหลด `.env` ผ่าน dotenv + migrate adapter

## Test Result
- `bunx tsc --noEmit` → **0 errors**
- `bunx prisma generate` → success (Prisma Client v7.8.0)
- Smoke test:
  - `GET /health` → **HTTP 200** `{success:true,data:{status:"ok"}}`
  - `GET /health/ready` → **HTTP 200** `{success:true,data:{db:"ok"}}` (Postgres connect OK)
  - `GET /does-not-exist` → **HTTP 404** `{success:false,error:{code:"NOT_FOUND",message:"ไม่พบ API endpoint นี้"}}`
  - `GET /swagger` → **HTTP 200** (swagger UI mount)
  - Pino structured log render ปกติ + Asia/Bangkok timestamp

## Notes
- DB: `smart_pole` บน Postgres 18 instance (port 5432) — credential ตาม pmk-psom-v2-remark (`postgres/gg0943455931`)
- TimescaleDB extension **ยังไม่ติดตั้ง** — ติดตั้งทีหลัง E08 ใช้ Postgres ปกติไปก่อน + เพิ่ม `create_hypertable()` migration เสริมเมื่อ Timescale พร้อม
- env ที่ optional ใส่ default ใน schema — ห้าม `process.env.X || "fallback"` กระจาย
