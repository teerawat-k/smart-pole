# T07 · Audit service module — fire-and-forget + viewer endpoint

Epic: E00
Status: done
Blocked by: T02,T04

## Expected
- `src/modules/audit/` ตาม atomic pattern (repository + service + constants + controller + index)
- Prisma model `AuditLog` — fields ตาม pmk style (`userId, action, module, targetId, payload Json, createdAt`)
- service `log()` fire-and-forget (ห้าม await)
- service `list()` + controller `GET /api/audit-logs` สำหรับ frontend viewer (E22)

## Summary
- เพิ่ม `AuditLog` model + migration `init_audit_log`
- pattern ตาม pmk-psom-v2-remark — simple `{ userId, action, module, targetId, payload? }` (ไม่มี ipAddress/requestId field — เก็บใน payload ถ้าต้อง)
- `auditService.log()` — sync void, ภายใน `.catch()` log error ผ่าน pino (ไม่ throw, ไม่ block)
- `auditService.list()` — repository pagination + filter (module/action/userId/targetId/dateRange)
- controller `GET /api/audit-logs` — เปิด public ตอนนี้ (TODO เพิ่ม authPlugin + permission `audit_log:canView` ตอน E02 พร้อม)
- constants: `AuditAction` (CREATE, UPDATE, DELETE, STATUS_CHANGE, REORDER, LOGIN, LOGOUT, RESET_PASSWORD, CHANGE_PASSWORD, UNLOCK, MQTT_INGEST_FAIL, SUBMIT, APPROVE, REJECT, CANCEL, TRANSITION) + `SYSTEM_USER_ID = 0`

## Test Result
- `bun test src/modules/audit/audit.service.test.ts` → **5 pass / 0 fail** (319ms)
  - เรียก repository.create พร้อม payload
  - เรียกได้แม้ไม่มี payload
  - return void ทันที
  - log error เมื่อ repo fail
  - ไม่ throw แม้ repo fail
- `bun test src/modules/audit/audit.integration.test.ts` → **2 pass / 0 fail** (real DB)
  - write + query กลับ
  - write ไม่มี payload → row.payload = null
- Smoke: `GET /api/audit-logs?page=1&limit=5` → 200 `{success:true, data:[], total:0, page:1, limit:5}`
- All backend: **57 pass / 0 fail** (8 files, 127 expect calls)
- `bunx tsc --noEmit` 0 errors

## Notes
- frontend viewer ทำใน E22 — ใช้ endpoint นี้
- หาก future ต้องการ before/after diff: ใช้ payload field (Json) เก็บ `{ before, after }`
