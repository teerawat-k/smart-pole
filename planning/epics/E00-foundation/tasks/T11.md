# T11 · Backend test setup — bunfig + integration helpers

Epic: E00
Status: done
Blocked by: T02,T07

## Expected
- `bunfig.toml` — preload `test-setup.ts` (ทำใน T01 แล้ว)
- `test-setup.ts` — set NODE_ENV=test (ทำแล้ว)
- `src/common/test-helpers/`
  - `expect-error.ts` — `expectError(promise, code)` (กัน Bun bug)
  - `test-prefix.ts` — generate unique prefix per test run
  - `index.ts` — re-export
- `package.json` scripts: `test`, `test:unit`, `test:integration`, `test:watch` (ทำแล้ว)

## Summary
- expectError: helper assert AppError + code match
- makeTestPrefix: `<scope>_test_<timestamp>_<rand3>` — รันทดสอบขนานกันได้ไม่ชน

## Test Result
- `bun test src/common/test-helpers/` → **5 pass / 0 fail**
- All backend: **84 pass / 0 fail** (161 expect calls, 11 files)
- `bunx tsc --noEmit` 0 errors

## Notes
- T01 setup `bunfig.toml` + `test-setup.ts` แล้ว — task นี้เพิ่ม helpers
- integration test ของ module ใช้ `await prisma.<model>.deleteMany({ where: { <field>: { startsWith: TEST_PREFIX } } })`
