# T11 · Backend test setup — bunfig + integration helpers

Epic: E00
Status: todo
Blocked by: T02,T07

## Expected
- `backend/bunfig.toml` — preload `test-setup.ts`
- `backend/test-setup.ts` — set NODE_ENV, clear mocks
- `backend/prisma/prisma.config.test.ts` — แยก DATABASE_URL ของ test
- `backend/src/common/test-helpers/`
  - `expect-error.ts` — `expectError(promise, code)` (กัน Bun bug `expect().rejects`)
  - `test-prefix.ts` — generate `TEST_PREFIX` per run
  - `cleanup.ts` — `cleanupAll()` + `cleanupModule(name)`
- `package.json` scripts:
  - `test` (all)
  - `test:unit` (`*.service.test.ts`)
  - `test:integration` (`*.integration.test.ts`)
  - `test:watch`

## Test Guide
- [ ] `bun test:unit` รันแยก, mock isolated ระหว่างไฟล์
- [ ] `bun test:integration` ใช้ test DB จริง + cleanup `afterAll`
- [ ] sample integration test ผ่าน

## Notes
- ระบุใน CLAUDE.md: unit + integration **ต้องรันแยกไฟล์** (mock.module leak ใน Bun)
- test DB: docker-compose แยก service `postgres-test` port 5433
