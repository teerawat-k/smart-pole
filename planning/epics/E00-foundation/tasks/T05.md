# T05 · Common schemas — pagination, reorder, password

Epic: E00
Status: todo
Blocked by: T01

## Expected
- `src/common/schemas/pagination.ts` — `paginationQuery` (TypeBox) + `PaginationQuery` type
- `src/common/schemas/reorder.ts` — `reorderSchema` + `ReorderInput` type
- `src/common/schemas/password.schema.ts` — Zod password rule (≥ 8 chars, ตัวเลข + ตัวอักษรใหญ่/เล็ก) + thai message

## Test Guide
- [ ] pagination default page=1 limit=20
- [ ] limit > 100 → validation error
- [ ] sortOrder ต้องเป็น `asc` หรือ `desc` เท่านั้น
- [ ] password < 8 → error
- [ ] password missing uppercase → error

## Notes
- เผื่ออนาคตเพิ่ม `cursor` pagination → ขยาย schema ไม่ใช่ทำ schema ใหม่
