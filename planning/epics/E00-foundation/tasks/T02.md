# T02 · Prisma plugin — adapter + Decimal serialize + singleton

Epic: E00
Status: done
Blocked by: T01

## Expected
- `src/plugins/prisma.ts` — singleton PrismaClient + PrismaPg adapter
- `Decimal.prototype.toJSON` override → number
- Type `PrismaTx` ใน `common/utils/prisma-tx.ts`
- `db:generate`, `db:migrate`, `db:deploy`, `db:seed`, `db:studio` scripts
- `prisma/schema.prisma` + `prisma/prisma.config.ts`

## Summary
- prisma plugin ทำใน T01 แล้ว — task นี้เพิ่ม unit test ครอบ:
  - `Decimal.toJSON` 4 cases (basic / integer / precision / negative)
  - singleton 2 cases (re-import / globalThis cache)
- `pingDb()` helper export เพื่อใช้ใน health check

## Test Result
- `bun test src/plugins/prisma.test.ts` → **6 pass / 0 fail** (7 expect calls, 405ms)
- `bunx prisma generate` → success (Client v7.8.0)

## Notes
- Pool config: ใช้ default ของ PrismaPg adapter (ไม่ต้อง pg.Pool แยกใน Prisma 7)
- Production: ไม่ตั้ง `globalThis.__prisma` — ป้องกัน memory leak
