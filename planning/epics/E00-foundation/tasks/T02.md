# T02 · Prisma plugin — adapter + Decimal serialize + singleton

Epic: E00
Status: todo
Blocked by: T01

## Expected
- `src/plugins/prisma.ts` — singleton PrismaClient + PrismaPg adapter + pg.Pool
- `Decimal.prototype.toJSON` override → number (test ด้วย field tests)
- Type `PrismaTx` export จาก `common/utils/prisma-tx.ts`
- `db:generate`, `db:migrate`, `db:deploy`, `db:seed`, `db:studio` scripts
- `prisma/schema.prisma` — base config (provider postgresql + driverAdapters)
- `prisma/prisma.config.ts`

## Test Guide
- [ ] `bun run db:generate` สำเร็จ
- [ ] singleton: 2 import = instance เดียว (ตรวจด้วย `prisma === prisma2`)
- [ ] Decimal `new Decimal("12.34")` JSON.stringify → `12.34` (number, ไม่ใช่ string)
- [ ] reload dev server ไม่สร้าง connection ใหม่ (ใช้ globalThis cache)

## Notes
- Production: ไม่ตั้ง `globalThis.__prisma`
- Pool config: `max: 10`, `idleTimeoutMillis: 30_000`
