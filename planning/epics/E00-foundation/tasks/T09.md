# T09 · File storage plugin — sanitize + buildPath + fs helpers

Epic: E00
Status: done
Blocked by: T01

## Expected
- `src/plugins/storage.ts` — atom helpers:
  - `sanitizeFilename(name)` — strip path traversal + non-ASCII + limit 200
  - `buildStoragePath(module, id, fileName)` → `uploads/{module}/{year}/{month}/{id}-{ts}-{safe}`
  - `ensureDir(filePath)` — recursive mkdir
  - `fileExists(path)` / `deleteFile(path)` — fs helper สำหรับ E09 (camera archive)

## Summary
- pure helper functions ไม่มี state — testable แยกได้
- ใช้ใน upload module (avatar/profile pic) + E09 camera archive (delete cron)
- MinIO ตัดออก — ใช้ file system volume mount แทน

## Test Result
- `bun test src/plugins/storage.test.ts` → **11 pass / 0 fail** (78ms)
  - sanitize 5 cases (path traversal, backslash, non-ascii, ascii, length limit)
  - buildStoragePath 2 cases (pattern + sanitize integration)
  - fs helpers 4 cases (ensureDir nested + fileExists true/false + deleteFile + delete missing)

## Notes
- เก็บ relative path ใน DB เสมอ — `data/uploads/...` หรือ `data/recordings/...`
- non-ascii (ภาษาไทย) → `_` (1 character per code point)
