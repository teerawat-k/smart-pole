# T09 · File storage plugin — buildStoragePath + ensureDir

Epic: E00
Status: todo
Blocked by: T01

## Expected
- `src/plugins/storage.ts` — atom helpers
  - `buildStoragePath(module, id, fileName)` → `uploads/{module}/{year}/{month}/{id}-{ts}-{safeName}.ext`
  - `ensureDir(filePath)` — recursive mkdir
  - `sanitizeFilename(name)` — strip `../`, limit ≤ 255, allow `[\w.\-]` only
- (Phase 2) เพิ่ม MinIO client — ทำใน E09 ไม่ทำที่นี่

## Test Guide
- [ ] `sanitizeFilename("../../etc/passwd")` → `etc_passwd`
- [ ] `buildStoragePath("avatar", 5, "ทเดสต.jpg")` returns `uploads/avatar/2026/04/5-{ts}-_.jpg` (non-ascii → `_`)
- [ ] ensureDir สร้าง dir nested ได้

## Notes
- เก็บแค่ relative path ใน DB (ห้ามเก็บ absolute)
