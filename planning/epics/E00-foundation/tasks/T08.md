# T08 · RBAC middleware skeleton — requirePermission + cache layer

Epic: E00
Status: todo
Blocked by: T02,T04

## Expected
- `src/common/middleware/rbac.ts` — `requirePermission(module, action)` Elysia plugin
- `src/common/middleware/rbac.repository.ts` — `findPermissionsByRoleId(roleId)`
- In-memory cache (Map<roleId, Set<"module:action">>) TTL 5 min
- Invalidate cache เมื่อ permission update (ผ่าน event emitter — implement ใน E03)
- Permission key format: `module:action` (เช่น `pole:create`, `user:delete`)

## Test Guide
- [ ] role ที่ไม่มี permission → 403
- [ ] role ที่มี permission → next handler
- [ ] cache hit ใน request ที่ 2 (ไม่ query DB) — verify ด้วย mock prisma call count
- [ ] TTL หมด → query DB ใหม่
- [ ] invalidate → clear แค่ roleId เดียว ไม่กระทบ role อื่น

## Notes
- **GET /lookup + GET /** ใช้ auth-only ไม่ตรวจ permission (ตามมาตรฐาน)
- ลด DB query — production มี mutation ~5%, GET ~95%
- เตรียม hook สำหรับ E03 invalidate
