# T08 · RBAC middleware skeleton — requirePermission + cache layer

Epic: E00
Status: done
Blocked by: T02,T04

## Expected
- `rbac.ts` — `hasPermission(roleName, "module:action")` + `requirePermission(...)` Elysia beforeHandle + `invalidatePermissionCache()`
- `rbac.repository.ts` — `fetchRoleWithPermissions(roleName)` (skeleton return null จนกว่า E02 ทำ Role schema)
- In-memory cache `Map<roleName, CachedRole>` — invalidate per role / global
- Permission key: `module:action` (lowercase kebab — `pole:view`, `pole:create`)

## Summary
- pattern ตาม pmk-psom-v2-remark
- `isSystem` role = bypass ทุก permission
- cache: ไม่ใช้ TTL (invalidate ทันทีเมื่อ E03 emit `roleChanged` event — เพิ่ม wiring ใน E02.T04)
- `requirePermission(perm)` return Elysia beforeHandle async — รับ context + ตรวจ user.role → throw ForbiddenError ถ้าไม่ผ่าน
- repository ตอนนี้ return null (Role/RolePermission schema ยังไม่มี) → fail-safe (default false)

## Test Result
- `bun test src/common/middleware/rbac.test.ts` → **11 pass / 0 fail** (48ms)
  - hasPermission: isSystem true / has perm / no perm / role missing / cache works
  - invalidate: per-role + clear all
  - requirePermission: missing user / isSystem / has perm / no perm
- All backend: **68 pass / 0 fail** (140 expect calls)
- `bunx tsc --noEmit` 0 errors

## Notes
- E02 + E03 จะเพิ่ม Role/RolePermission schema → uncomment query ใน rbac.repository.ts
- `GET /lookup` + `GET /` (list): ใช้ auth-only ไม่ตรวจ permission (ตามมาตรฐาน — list ส่ง flag canEdit/canDelete พร้อม)
