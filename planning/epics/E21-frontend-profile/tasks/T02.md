# T02 · Change password
Epic: E21

## Expected
- dialog: currentPassword + newPassword + confirmNewPassword
- Zod validate (matches + policy)
- success → toast + force logout (เพราะ refresh tokens revoked) → redirect login
