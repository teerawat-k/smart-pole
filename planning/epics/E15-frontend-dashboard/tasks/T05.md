# T05 · Realtime updates

Epic: E15

## Expected
- ใช้ `useWebSocket()` invalidate `[dashboard, poleId]`, `[alert, poleId]`, `[pole-status]` ตาม message type
- ห้ามใช้ `refetchInterval`
- show "live" indicator (pulse dot) เมื่อ WS connected

## Test Guide
- [ ] mqtt sensor → ภายใน 1s frontend update
- [ ] WS disconnect → แสดง indicator offline
