# T04 · Active alerts banner

Epic: E15

## Expected
- `components/dashboard/alerts-banner.tsx`
- list active alerts (isResolved=false) ของ pole นั้น
- click → AlertDialog "ยืนยันการ resolve" → `POST /api/alerts/:id/resolve`
- realtime: ผ่าน WS invalidation

## Test Guide
- [ ] new alert → banner update (ผ่าน WS)
- [ ] resolve → banner ลด
