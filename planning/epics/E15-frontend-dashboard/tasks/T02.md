# T02 · Sensor cards + threshold color

Epic: E15

## Expected
- `components/dashboard/sensor-card.tsx` — atom (display-only, props in)
- props: `label, value, unit, threshold?, severity?, icon?`
- color: green (normal), amber (warning), red (critical) — derived from severity prop ที่ backend ส่งมา (ห้าม frontend คำนวณเอง)

## Test Guide
- [ ] severity=critical → red
- [ ] no value → "—"
