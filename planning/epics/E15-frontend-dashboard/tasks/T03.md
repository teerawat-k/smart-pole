# T03 · HLS video player

Epic: E15

## Expected
- `components/dashboard/hls-player.tsx` — lazy load with `next/dynamic({ ssr: false })`
- ใช้ `hls.js` (มีใน package.json legacy แล้ว — ใช้ต่อ)
- props: `src, autoPlay?, controls?`
- error handler: stream offline → fallback poster + retry button
- destroy hls instance on unmount

## Test Guide
- [ ] play/pause control
- [ ] stream offline → fallback message
- [ ] memory leak ไม่มี (mount/unmount 10 ครั้ง)
