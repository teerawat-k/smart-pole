# T01 · Schema split

Epic: E12

## Expected
- `SystemLog`: `id, logType (enum), userId?, usernameSnap, ipAddress, userAgent, sessionId?, pageKey?, action?, detail Json?, createdAt`
- `AuditLog`: `id, userId, action, targetType, targetId, before Json?, after Json?, requestId, ipAddress, createdAt`
- enum `SystemLogType { LOGIN_SUCCESS LOGIN_FAIL LOGOUT CAPTCHA_FAIL PAGE_ACCESS }`
- indexes ตามที่ filter บ่อย

## Test Guide
- [ ] migration apply
- [ ] insert ทั้ง 2 table
