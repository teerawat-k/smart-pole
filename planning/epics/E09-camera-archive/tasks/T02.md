# T02 · VideoRecording schema + module

Epic: E09

## Expected
- model `VideoRecording`:
  - `id BigInt`, `poleId`, `filename @unique`, `storagePath`, `recordedDate Date`, `startTime`, `endTime`, `durationSec`, `fileSizeBytes`
  - standard audit fields (`isActive`, soft delete, createdBy/updatedBy)
  - indexes: `(poleId, recordedDate desc)`, `(deletedAt)`
- module ตาม pattern
- endpoints:
  - `GET /api/recordings?poleId&date&page&limit`
  - `GET /api/recordings/:id`
  - `GET /api/recordings/:id/url` — presigned URL
  - `DELETE /api/recordings/:id` (soft + ถ้า admin = hard delete file ใน MinIO ผ่าน job)

## Test Guide
- [ ] CRUD ครบ
- [ ] list filter by date + pole
- [ ] /url return presigned + log audit `RECORDING_PLAYBACK`
