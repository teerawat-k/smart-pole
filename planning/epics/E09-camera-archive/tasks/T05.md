# T05 · List recordings + filter

Epic: E09

## Expected
- `GET /api/recordings?poleId&date&minDuration&page&limit&sortBy&sortOrder`
- default sort: `recordedDate desc, startTime desc`
- pagination + total
- list ส่ง field minimal (`id, filename, recordedDate, startTime, endTime, durationSec, fileSizeBytes`)
- detail (`/:id`) เพิ่ม pole info

## Test Guide
- [ ] filter by date range
- [ ] minDuration filter (e.g. ≥ 60s)
- [ ] pagination ถูก
