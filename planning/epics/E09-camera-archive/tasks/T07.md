# T07 · HLS live URL builder

Epic: E09

## Expected
- helper `buildHlsUrl(poleName)` → `${env.SRS_HLS_BASE}/live/${poleName}.m3u8`
- ใช้ใน dashboard endpoint (E15)
- ห้าม hardcode URL ใน frontend — ใช้จาก backend

## Test Guide
- [ ] return URL ที่ frontend HLS player เล่นได้
- [ ] env.SRS_HLS_BASE missing → fallback localhost:8080
