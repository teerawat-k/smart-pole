# T01 · Storage strategy + volume mount + path convention

Epic: E09

## Expected
- volume `./data/recordings` (mount ทั้ง backend และ SRS)
- path convention: `recordings/{poleName}/{YYYY-MM-DD}/{HHMM}-{HHMM2}.mp4`
- `src/plugins/storage.ts` (extend จาก E00.T09):
  - `interface StorageProvider { exists, stat, delete, readStream }`
  - implement `LocalFileStorage` (default)
  - placeholder `S3Storage` (สำหรับอนาคต)
- env: `STORAGE_PROVIDER=local`, `STORAGE_RECORDINGS_PATH=./data/recordings`
- Elysia static plugin mount `/recordings` → readonly serve + JWT guard

## Test Guide
- [ ] StorageProvider interface implemented
- [ ] file existing returns true, missing → false
- [ ] static serve ผ่าน /recordings/:path (มี token)
- [ ] ไม่มี token → 403
