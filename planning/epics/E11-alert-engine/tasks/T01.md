# T01 · Alert schema + module + lookup

Epic: E11

## Expected
- model `Alert`:
  - `poleId, alertType (enum), severity (enum), message, value Decimal?, threshold Decimal?, isResolved, resolvedAt?, resolvedById?, triggeredAt`
  - audit fields
  - indexes: `(poleId, isResolved, triggeredAt desc)`, `(severity)`, `(alertType)`
- enum `AlertType`, `AlertSeverity`
- module ตาม pattern + `/lookup` (alertType list สำหรับ filter)

## Test Guide
- [ ] CRUD ครบ
- [ ] enum validation
