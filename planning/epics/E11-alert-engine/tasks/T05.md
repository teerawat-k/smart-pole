# T05 · Notification module + recipient resolver

Epic: E11

## Expected
- model `Notification`: `userId, type, title, message, refType, refId, isRead, readAt?, createdAt`
- module ตาม pattern
- `POST` ภายใน + `GET /api/notifications?isRead&page&limit`
- `PATCH /api/notifications/:id/read`, `POST /api/notifications/read-all`
- atom `flow/resolve-recipients.ts` — return user[] ตาม rule:
  - severity=critical → ทุก admin
  - severity=warning → ทุก admin
  - (future) per-pole subscription
- WS push + bell badge count

## Test Guide
- [ ] alert critical → admin ทุกคนได้ notification + ws push
- [ ] mark read → badge count ลด
- [ ] integration test
