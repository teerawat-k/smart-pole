# T03 · alertService.resolve()

Epic: E11

## Expected
- `flow/resolve.ts` atom — set `isResolved=true, resolvedAt, resolvedById`
- 2 entry points:
  - manual: `POST /api/alerts/:id/resolve` body `{ note? }`
  - auto: เรียกจาก E07 (offline → online), E11 evaluate-rule (ถ้าค่าลงต่ำกว่า threshold)
- audit + WS

## Test Guide
- [ ] manual resolve → row updated
- [ ] auto resolve → resolvedById = SYSTEM_USER_ID
