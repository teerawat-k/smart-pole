# T02 · createFromMqtt + dedupe

Epic: E11

## Expected
- `flow/create-from-mqtt.ts` atom
- ตรวจ duplicate: query `WHERE poleId AND alertType AND isResolved=false AND triggeredAt > now() - dedupeWindow`
- ถ้ามี → ไม่สร้างใหม่ (return existing) + update `value, threshold` ถ้าเปลี่ยน
- ถ้าไม่มี → create + broadcast WS + send notification

## Test Guide
- [ ] alert ใหม่ → 1 row + ws + notification
- [ ] alert ซ้ำ ≤ 60s → ไม่สร้าง
- [ ] alert ซ้ำ > 60s → สร้างใหม่
