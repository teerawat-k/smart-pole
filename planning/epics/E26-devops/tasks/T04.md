# T04 · SRS config + DVR callback
Epic: E26

## Expected
- `infra/srs/srs.conf` — RTMP 1935 + HLS 8080 + DVR hook URL → backend `/api/srs/dvr`
- DVR file path: `{pole}/{date}/{time}.mp4` ใน volume mount + serve ผ่าน backend static (ไม่ใช้ MinIO)

## Test Guide
- [ ] push RTMP → HLS available + DVR file created
- [ ] backend ได้ callback → 1 row VideoRecording
