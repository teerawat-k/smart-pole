# T01 · Prisma schema — Module, RolePermission, Role

Epic: E02
Status: todo

## Expected
- เพิ่ม model `Module`, `RolePermission`, `Role` (ตาม schema ใน epic.md)
- Migration: `add_rbac_master`
- ทุก model มี audit fields ครบ (`isActive`, `deletedAt`, `createdBy`, `updatedBy`, `deletedBy`, `createdAt`, `updatedAt`)
- Index: `Role.name@unique`, `Module.key@unique`, `RolePermission.@@unique([roleId, moduleId])`

## Test Guide
- [ ] migration apply ผ่าน
- [ ] insert Module + Role + RolePermission ทำงาน
- [ ] cascade onDelete: ลบ Role → RolePermission ลบตาม
