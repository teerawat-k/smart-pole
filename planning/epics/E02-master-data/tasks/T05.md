# T05 · Permission helper — `permissionToFlags(role, module)`

Epic: E02
Status: todo
Blocked by: E02.T02

## Expected
- `src/common/utils/permission-flags.ts`
  - `permissionToFlags(roleId, moduleKey)` → `{ canView, canCreate, canEdit, canDelete, canExport }`
  - ใช้ใน list endpoint ของทุก module เพื่อส่ง flag พร้อม row
  - ใช้ cache เดียวกับ T04
- Pattern: list endpoint return `{ data: [...], permissions: { canCreate, canExport } }`

## Test Guide
- [ ] role ที่มี canEdit=true → flag canEdit=true
- [ ] role ไม่มี permission ของ module → ทุก flag false
- [ ] performance: ไม่ query DB ทุกครั้ง (cache hit)
