# T02 · Permission listing endpoint (อยู่ใน Role module)

Epic: E02
Status: done
Blocked by: E02.T01

## Note (เปลี่ยนจาก plan เดิม)
- Plan เดิม: "Module module CRUD" + endpoint แยก
- ปรับใหม่: ใช้ Permission table flat (ตาม pmk-psom-v2-remark) — **ไม่มี CRUD UI**
- Permission seed อย่างเดียวจาก code (`prisma/seeds/permissions.ts`)
- listing endpoint ย้ายไปอยู่ใต้ Role module (`GET /api/roles/permissions`) เพื่อ frontend ใช้ใน Role permission editor

## Endpoint
- `GET /api/roles/permissions` → array of permissions ทั้งหมด (id, module, action, category, labels)

## Test Result
- ทำใน E03.T01 (Role module)
- Smoke: `GET /api/roles/permissions` → 26 permissions
- 14 unit tests pass

## Notes
- Permission CRUD จะเปิดเฉพาะถ้าจำเป็น (yagni)
- เพิ่ม module ใหม่ → แก้ `prisma/seeds/permissions.ts` → re-run seed
