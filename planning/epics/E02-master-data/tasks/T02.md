# T02 · Module module — CRUD + lookup

Epic: E02
Status: todo
Blocked by: E02.T01

## Expected
- `src/modules/module/` ตาม atomic pattern (controller/service/repository/schema/constants/index/test)
- Endpoints:
  - `GET /api/modules` — list
  - `GET /api/modules/lookup` — id+displayName (ใช้ใน Permission UI)
  - `GET /api/modules/:id`
  - `POST /api/modules`
  - `PATCH /api/modules/:id`
  - `DELETE /api/modules/:id` (soft)
  - `PATCH /api/modules/reorder`
  - `PATCH /api/modules/:id/toggle-active`
- SELECTs: `MODULE_LIST_SELECT`, `MODULE_DETAIL_SELECT`, `MODULE_LOOKUP_SELECT`
- Permissions: `module:view`, `module:create`, `module:edit`, `module:delete`

## Test Guide
- [ ] CRUD ครบ + audit log
- [ ] reorder ใช้ `reorderRecord()` helper
- [ ] lookup return id + displayName เท่านั้น
- [ ] duplicate key → DuplicateError
- [ ] integration test 6 cases

## Notes
- Module ที่ system สร้างจาก seed ห้ามลบ (`isSystem` flag ภายใน — เพิ่ม field)
