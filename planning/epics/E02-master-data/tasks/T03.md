# T03 · Seed permissions + roles + admin user

Epic: E02
Status: done
Blocked by: E02.T01

## Expected
- `prisma/seeds/permissions.ts` — idempotent seed (upsert by `[module, action]`)
- `prisma/seeds/roles.ts` — admin (isSystem) + user (isSystem + permissions)
- `prisma/seeds/users.ts` — default admin user (`admin/Admin@1234`)
- `prisma/seeds/index.ts` — orchestrator (permissions → roles → users)

## Summary
- 26 permissions seeded:
  - Monitoring: dashboard (view), camera_archive (view/delete/export), sensor_archive (view/export), alert (view/edit)
  - Master: pole (view/create/edit/delete)
  - Admin: user/role (view/create/edit/delete) + audit_log/system_log (view/export)
  - Personal: my_profile (view/edit)
- 2 system roles: admin (bypass), user (read-only monitoring + edit profile)
- default admin: id=1, username=admin, password=Admin@1234 (argon2id hash)

## Test Result
- `bun prisma/seeds/index.ts` (รัน 2 ครั้งติด) → idempotent
  - run 1: Seeded 26 permissions / Roles / Default admin created (id=1)
  - run 2: Seeded 26 permissions / Roles / Admin user already exists (id=1)
- All 84 backend tests pass

## Notes
- legacy SQL dump (project-backup/SmartPole_Database.sql) ใช้เป็น reference แล้ว
- legacy: 8 pages → ของเรา 11 modules (เพิ่ม alert, audit_log แยก system_log)
- legacy admin user ใช้ bcrypt — เราใช้ argon2id
