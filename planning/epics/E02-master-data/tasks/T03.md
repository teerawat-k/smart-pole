# T03 · Seed permissions — `seeds/permissions.ts`

Epic: E02
Status: todo
Blocked by: E02.T01,T02

## Expected
- `prisma/seeds/permissions.ts` — idempotent seed
- Seed `Module` ตาม functional ของระบบ:
  - `dashboard`, `camera_archive`, `sensor_archive`, `my_profile`
  - `module`, `role`, `user`, `pole`, `alert`, `audit_log`, `system_log`
- Seed `Role` system: `admin`, `user` (`isSystem: true`)
- Seed `RolePermission`:
  - admin: ทุกหน้า ทุก action = true
  - user: dashboard/camera_archive/sensor_archive canView only, my_profile canView+canEdit
- Seed default admin user `admin/Admin@1234` (E04 จะใช้)
- ทำใน `seeds/index.ts` order: modules → roles → permissions → users

## Test Guide
- [ ] รัน 2 ครั้ง = ผลเหมือนเดิม (idempotent)
- [ ] admin role ใช้ pole:create ได้
- [ ] user role ใช้ pole:create ไม่ได้
- [ ] integration test seed
