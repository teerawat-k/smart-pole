# T03 · Permission matrix editor
Epic: E20

## Expected
- table: row=module, col=action (5 checkboxes)
- header checkboxes: select all per col
- row checkbox: select all per row
- save = `PUT /api/roles/:id/permissions`
- isSystem role → read-only mode + banner
