# T06 · LWT (last-will) handling

Epic: E06

## Expected
- เสาทุกต้น set LWT topic `smartpole/{poleName}/heartbeat` payload `{"status":"offline"}` QoS 1
- เมื่อเสา ungraceful disconnect → broker ส่ง LWT → handle-heartbeat ตรวจ `status === "offline"` → `poleService.markOffline(poleName)`
- broadcast WebSocket

## Test Guide
- [ ] disconnect ไม่ส่ง DISCONNECT → backend ได้ LWT → mark offline
- [ ] graceful disconnect → ไม่ trigger LWT
