# T04 · Handler — handle-heartbeat.ts

Epic: E06

## Expected
- `handle-heartbeat.ts` atom
- เรียก `poleService.recordHeartbeat({ poleName, signalDbm, uptimeSec, timestamp })`
- update `pole.poleStatus = 'online'`, `lastSeenAt`
- write Influx `pole_heartbeat` (E08)
- broadcast WebSocket `{ type: 'pole-status-changed', poleName, status: 'online' }`

## Test Guide
- [ ] heartbeat → status online + ws broadcast
- [ ] pole offline → online → broadcast 1 ครั้ง (ไม่ broadcast ทุก heartbeat)
- [ ] integration test
