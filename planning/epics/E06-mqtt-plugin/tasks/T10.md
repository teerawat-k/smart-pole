# T10 · Unit tests with mock MQTT broker

Epic: E06

## Expected
- `aedes` หรือ `mqtt-server` — embed broker ใน test
- helper `withMockBroker(fn)` — start broker → connect client → run test → cleanup
- test ทุก handler atom + dispatcher
- integration test ครอบ flow ทั้งหมด

## Test Guide
- [ ] mock broker test isolated (port random)
- [ ] coverage ≥ 80% ของ plugin/mqtt
