# T03 · Handler — handle-sensor.ts

Epic: E06

## Expected
- `src/plugins/mqtt/handlers/handle-sensor.ts` atom
- เรียก `sensorArchiveService.write(...)` (E08)
- อัพเดท `pole.lastSeenAt = timestamp` (ผ่าน `poleService.touch()`)
- audit log: `MQTT_INGEST_SENSOR` (only error case — ไม่ log success ทุก message — too noisy)

## Test Guide
- [ ] valid → write Influx + update lastSeenAt
- [ ] pole ไม่มีใน DB → log warn + skip
- [ ] sensor service throw → handler catch + log error
