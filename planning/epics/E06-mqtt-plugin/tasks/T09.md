# T09 · Replay buffer + dead-letter

Epic: E06

## Expected
- Handler fail → push to in-memory `mqttDlq` (max 1000) + log
- `GET /api/admin/mqtt/dlq` (admin) — list dead letters
- `POST /api/admin/mqtt/dlq/:id/replay` — re-dispatch

## Test Guide
- [ ] handler throw → message ใน dlq
- [ ] replay → reprocess
- [ ] dlq เต็ม → drop เก่าที่สุด
