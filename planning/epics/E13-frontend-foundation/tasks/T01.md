# T01 · Frontend bootstrap — Next + Tailwind + shadcn

Epic: E13

## Expected
- `bun install` package.json (มีอยู่แล้ว)
- Tailwind config + globals.css ใช้งานได้
- `bunx shadcn init` (ใช้ `components.json` ที่ตั้งไว้)
- font: ใช้ system + Sarabun สำหรับภาษาไทย
- `bun dev` ขึ้นที่ port 7765

## Test Guide
- [ ] dev server up + hot reload
- [ ] Tailwind class ทำงาน
- [ ] shadcn add button → import + render ได้
