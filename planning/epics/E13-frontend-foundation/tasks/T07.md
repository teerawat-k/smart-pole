# T07 · AppCombobox + AppDatePicker + AppFileUpload

Epic: E13

## Expected
- `components/layout/app-combobox/` — based on shadcn Command
  - props: `options, value, onChange, placeholder, searchable, multi?, lookupHook?`
  - **lookupHook** = function ที่ return `{ data, isLoading }` — รับ search query → call `/lookup` (lazy + debounced)
  - ห้าม feed list endpoint
- `components/layout/app-date-picker.tsx` — react-day-picker + Popover, format `YYYY-MM-DD`
- `components/layout/app-file-upload.tsx` — upload เมื่อ form submit (ไม่ auto), preview, validate MIME/size

## Test Guide
- [ ] combobox search debounce 400ms
- [ ] combobox empty option "ทั้งหมด" สำหรับ filter mode
- [ ] datepicker format YYYY-MM-DD
- [ ] file upload reject MIME ไม่ถูก
