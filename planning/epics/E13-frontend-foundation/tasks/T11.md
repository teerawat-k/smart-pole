# T11 · useDebounce, useAppAlertDialog, useRouteGuard, usePermission

Epic: E13

## Expected
- `hooks/use-debounce.ts` (มี — confirm ใช้ได้)
- `hooks/use-app-alert-dialog.tsx` — return `{ confirm }` + render dialog ที่ provider level
- `hooks/use-route-guard.ts` — redirect ถ้าไม่ login / ไม่มี permission
- `hooks/use-permission.ts` — `hasPermission(key)` from authStore + helpers `useHasModulePermission(module, action)`

## Test Guide
- [ ] guard redirect ทำงาน
- [ ] hasPermission ตอบถูก
