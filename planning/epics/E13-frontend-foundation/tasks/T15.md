# T15 · NotificationBell + toast wiring

Epic: E13

## Expected
- `components/shared/notification-bell.tsx` — header bell with badge (unread count)
- click → popover แสดง 10 รายการล่าสุด + "ดูทั้งหมด"
- "ทำเครื่องหมายอ่านทั้งหมด" button
- toast ใช้ sonner — success/error/warning ตาม API response
- WebSocket push notification → toast + bell badge update

## Test Guide
- [ ] new notification → bell badge +1 + toast
- [ ] mark as read → badge -1
