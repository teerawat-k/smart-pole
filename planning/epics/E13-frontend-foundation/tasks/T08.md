# T08 · AppDialog + AppAlertDialog

Epic: E13

## Expected
- `components/layout/app-dialog.tsx` — wrapper shadcn Dialog
  - sticky footer นอก scroll area
  - mode: `create | edit | view`
  - prevent close on outside click (`onInteractOutside`)
- `components/layout/dialog-form-footer.tsx` — submit + cancel buttons + isPending
- `components/layout/app-alert-dialog.tsx` + hook `useAppAlertDialog().confirm({ title, description, onConfirm })`
- ห้ามใช้ `window.confirm()` ทั้งระบบ

## Test Guide
- [ ] click outside → ไม่ปิด
- [ ] submit + isPending → spinner + disable
- [ ] alert dialog confirm flow
