# T04 · Zustand auth-store

Epic: E13

## Expected
- `stores/auth-store.ts` — state `{ user, accessToken, refreshToken, permissions }` + actions `setAuth`, `reset`, `updateProfile`
- persist key: `${PROJECT_PREFIX}-auth`
- `skipHydration: true` + rehydrate ใน providers
- `stores/index.ts` — `resetAllStores()`
- selectors: `useUser`, `useAccessToken`, `useHasPermission(key)`

## Test Guide
- [ ] persist ผ่าน reload
- [ ] skipHydration prevent hydration mismatch
- [ ] logout → store clear + localStorage clear
