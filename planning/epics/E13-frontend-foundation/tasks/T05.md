# T05 · shadcn install components หลัก

Epic: E13

## Expected
- รัน `bunx shadcn add` ทุก primitive ที่จะใช้:
  - button, input, label, textarea, select (จะใช้ผ่าน AppCombobox), checkbox, switch
  - dialog, alert-dialog, sheet, popover, tooltip
  - table, tabs, badge, separator, skeleton
  - form, dropdown-menu, scroll-area, command (สำหรับ AppCombobox)
- ห้ามแก้ไฟล์ใน `components/ui/`

## Test Guide
- [ ] ทุก component import + render ใน demo page
