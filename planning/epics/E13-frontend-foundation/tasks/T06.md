# T06 · AppForm + AppFormField

Epic: E13

## Expected
- `components/layout/app-form.tsx` — RHF wrapper + zodResolver + onSubmit handler
- `components/layout/app-form-field.tsx` — Field with label + required indicator + error styling (เปลี่ยนสี border + label, ไม่แสดง message สำหรับ required)
- props: `name, label, required, control, render`
- ใช้กับ AppCombobox, AppDatePicker, Input ได้ uniform

## Test Guide
- [ ] required + ไม่กรอก → label + border สีแดง
- [ ] error custom (DuplicateError) → แสดง message
- [ ] view mode → render plain text
