# T09 · DataTable component

Epic: E13

## Expected
- `components/layout/data-table/` — based on TanStack Table v8
  - server-side pagination/sorting/filtering (ห้าม client-side)
  - column definition with `sorter: true|false`
  - row double-click handler
  - action column `fixed: "right"` + DropdownMenu
  - `emptyText` prop
- `components/shared/column-visibility.tsx` — popover toggle (เก็บ localStorage)
- `components/shared/column-header-tooltip.tsx` — hover hint
- ห้าม `useQuery` ใน cell

## Test Guide
- [ ] sort 3-state (asc → desc → reset)
- [ ] pagination
- [ ] empty state แสดงเมื่อ data=0
- [ ] column visibility persist
