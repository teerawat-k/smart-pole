# T12 · useWebSocket hook

Epic: E13

## Expected
- `hooks/use-websocket.ts` — connect ที่ provider level
- รับ message type:
  - `invalidate` → `queryClient.invalidateQueries({ queryKey: [entity] })`
  - `notification` → toast + bell badge update
  - `pole-status-changed` → invalidate `pole` + `dashboard`
- reconnect แบบ exponential backoff (1s → 2s → 4s → 8s → 16s → 30s max)
- ส่ง `lastSeq` ตอน reconnect → resync (E10.T04)
- ping/pong handler

## Test Guide
- [ ] connect ผ่าน
- [ ] disconnect → reconnect
- [ ] invalidate ทำงาน — list refetch
- [ ] toast notification ขึ้น
